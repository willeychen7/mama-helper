# 项目日记

**只加不改。新的写在最上面。**

这个文件同时是变更记录、bug 台账和决策记录 —— 它们本质上是同一种东西：
按时间往下记的日记。分成三个文件没有意义。

| 标记 | 是什么 | 规矩 |
|---|---|---|
| ⚖️ | **决定** | 推翻时**新写一条**，说明推翻了哪条；旧那条留着，标题下加「已被 XX 取代」 |
| ✅ | **变更** | 记行为变化和新能力，不记重构 |
| 🐛 | **Bug** | 必须写**是被哪张图/哪个测试炸出来的** —— 这一列比其他列都有用 |

搜 `⚖️` 看所有决定，搜 `🐛` 看所有 bug。

## 决定一览

| | 决定 | 状态 |
|---|---|---|
| 01 | 金额和日期永远由本地抽取，不交给 LLM | 现行 |
| 02 | 脱敏用白名单方向，不用黑名单 | 现行 |
| 03 | 知识在构建期生成，不在运行期调用 | 现行 |
| 04 | 默认零网络，联网是用户主动开启的增强 | 现行（定位本身，不该推翻） |
| 05 | 默认不问 —— 只为金额和日期要求补拍，隐私字段一律沉默 | 现行（定位本身，不该推翻） |

---

## 2026-08-27 · ✅ 朗读 + 字号三档，以及改掉「不用 AI」的文案

**为什么现在做这个而不是继续修准确率**

对一个 70 岁、看不懂英文、也可能看不清小字的人，**朗读比准确率更重要** ——
她可能根本读不了我们辛苦拼出来的那句中文。

竞品 What's This Mail 的无障碍清单（朗读+调速、超大字、高对比度、VoiceOver）
等于免费送了一份需求文档。我们一条都没做。

**做了什么**

- `utils/speech.js` —— `buildSpeechText` 按老人关心的顺序拼朗读文本：
  诈骗警告 → 这是什么 → 谁寄的 → 大意 → 金额 → 期限 → 重拍提示 → 建议
- 「🔊 读给我听」大按钮，朗读中可切慢/正常/快三档语速（默认 0.85，比系统默认慢）
- 字号三档（标准 / 大 / 特大），用 CSS `zoom` —— 界面全是 Tailwind 绝对字号，
  在父层改 `font-size` 对 `text-2xl` 这类没有任何作用，只有 zoom 能整体放大
- 换一封信自动停止朗读

**一个隐私细节：不是所有语音都是本地的**

Chrome 桌面版有些中文语音是 Google 的云端语音 —— 念一句话要把文本发到服务器。
`pickChineseVoice()` **优先挑 `localService === true` 的**；
只找得到云端语音时，界面如实说明「点了会把小助手写的中文说明发给语音服务」，
让用户自己决定，而不是偷偷发出去。

念的是模板中文（结构上不含姓名地址），但那仍然是一次网络请求，
跟「零网络」的说法冲突。**如实说，比事后被发现好。**

**`speech.test.mjs`，16 条断言**

念错东西是隐私事故，不是体验问题 —— 老人多半不是一个人在房间里用这个 app。
所以核心断言只有一条：**朗读只念 layer0 的模板中文，绝不念 OCR 原文。**
六封真实账单逐一验证，姓名/地址/账号一个都不许出现。

**顺带改了结果页那句文案**

```
旧：以上内容全部由这台设备本机生成，没有联网，也没有交给任何 AI 模型。
新：信件照片不会上传。姓名、地址、账号这些个人信息，先在您自己的设备上处理掉。
    将来接上 AI 时，也只有去掉身份信息之后的内容才会发出去。
```

旧那句把一个**架构选择**说成了**产品承诺**，等于自己把手脚捆住 ——
将来接 AI 补长尾理解时，这句话就成了打自己的脸。

用户真正怕的从来不是「AI」这个词，是「我的信、地址、账号会不会被传走」。
**要卖的是边界，不是禁用。** 决定 04（默认零网络）不变，改的只是怎么说。

---

## 2026-08-27 · ⚖️ 决定 05 · 默认不问

**背景** 加「重拍提示」的时候发现一个矛盾：如果老人**故意**不拍地址和账号，
而 app 一直催他「请把整张单子拍全」—— 那这个卖点是「你的隐私归你」的产品，
就变成了一个天天催你交出隐私的东西。

**决定** **只为「要交多少」和「什么时候交」这两件事开口要求补拍。其余一概沉默。**

- 姓名、地址、账号缺了 —— 一个字都不说
- 机构名认不出 —— 显示「未知」，**但不催拍信头**

**理由**

1. **老人拍这张照片的目的就是想知道金额和日期。** 他绝不会故意把它们挡住 ——
   所以催这两样永远踩不到隐私。反过来，地址账号缺失多半是**故意**的。
2. **信头和收件人地址通常挨在一起**（信封窗口那块）。催拍信头等于可能催出地址，
   而认不出机构的代价小得多 —— 老人还是能看到要交多少、什么时候交。
3. 跟决定 02 是同一个方向：
   决定 02 是**默认不发**（没确认安全的不外发），这条是**默认不问**（没必要的不索取）。

**代价** 有些信会因为缺机构名而一直显示「未知」，准确率数字上不去。

**什么情况下该推翻**
不该。这是产品定位。将来一定会有人说「让老人补拍一下信头，机构识别率能从
60% 提到 85%」—— 那个收益不值得让 app 变成催人交隐私的东西。
要提高机构识别率，去接 LLM，别去问用户要。

---

## 2026-08-27 · ✅ 删掉二次 OCR，换成「告诉用户重拍」

**删了什么** `cropImageRegion` / `extractBestCropOCR` / `scoreCandidate` /
`chooseBetterOCRResult` / `runCriticalLineReOCR` / `refineCriticalOCRLines`
以及编排块和两块 UI 面板。**App.jsx 6298 → 5113 行，砍掉 1185 行。**

**为什么删 —— 三条理由**

1. **实测从没改善过任何一行**（`improved: 0`），存疑了整整一轮
2. **有确认的污染机制**：上下各扩 0.65 倍行高会吃到相邻行 →
   裁出来的所有 OCR item 被 `.join(' ')` 拼成一串 →
   分数高 5 分就替换掉原行。**为了修一个金额，可能把邻行文字塞进金额那一行**
3. **它想解决的场景，实测它解决不了** —— 见下

**第 3 条是这次新增的证据。** 用 WM 那封实拍账单直接测了二次 OCR 的核心假设
（裁出来放大能不能救回模糊的标签）：

```
全页 OCR：        「Your Payment is Due」整行没有输出
裁出来放大 4 倍：  认出来了 → "YourPayueitisvue"  置信度 82%
```

**字母上半截被照片边缘切掉了，放大只是把残缺放大。**
而且如果真把这行「补」进去，它匹配不上任何锚点，却会占一个数组位置 ——
`contentRedactor` 判断相邻是按数组下标的，等于往脱敏逻辑里插了一行噪音。

**换成了什么** `retakeHints` —— 系统本来就知道自己为什么失败
（「有一个很显眼的金额，但它没有任何锚点」），把这个原因翻译成老人能照做的一句话。

**这是唯一一个对所有信都有效的改动。** 老人重拍一次，标签完整了，问题消失。
实验证明：把那行标签补回原位，金额立刻变成「要交 87.05」。

---

## 2026-08-27 · 🐛 机构兜底把金额当成了机构名

**症状** WM 那封信显示「寄件机构：**$87.05**」。

**根因** 认不出机构时，兜底去挑「页面上方字号最大的一行」当机构名 ——
而这封信上字号最大的就是那个金额。

**第一版修法错了。** 我先用黑名单排除金额，结果它转头去挑了
「Visit wm.com/MyWM」；再排除网址，它还会去咬别的地方。**黑名单排不完。**

**改成白名单**：兜底挑出来的那一行必须**长得像机构名** ——
含机构词（Inc / District / Services / Waste / Management…），
或者整行大写且两个词以上（典型信头写法）。都不满足就显示「未知」。

跟决定 02 同一个取舍：白名单的代价是「更多信显示未知」，
黑名单的代价是「老人看到『寄件机构：$87.05』」。

**为什么这条值钱**：全美水电气垃圾公司几千家，机构词典永远补不完，
**兜底会一直被触发** —— 所以兜底自己必须是对的。这一条对每一封认不出机构的信都有效。

---

## 2026-08-27 · ✅ 第一封真实手机实拍进了测试

`demo_ocr_photo.json` + `fieldExtractor.photo.test.mjs`，6 条断言。

之前六封都是扫描件或网上公开样本。**这封是第一封真人用手机拍的**，
一上来就暴露了扫描件永远暴露不了的问题（见上面二次 OCR 那条）。

**它的通过标准和别的测试不一样**：不是「抽对金额」——
信息不全的照片，抽对了反而是瞎猜。三条标准是：

1. 绝不说错（不能报出任何金额或日期）
2. 绝不乱认（机构宁可「未知」）
3. 告诉老人怎么补救（给出重拍提示，且只针对金额和日期）

**这三条才是这个产品真正的及格线**，比「认出每一封信」现实得多，
而且是可以做到 100% 的。

---

## 2026-08-26 · 🐛 四封官方样本信，炸出五个 bug

按「OC 老人最常收、后果最重」找了四类官方样本，逐字原文做成
`fieldExtractor.tier1.test.mjs`：CMS 官方 EOB · OC 财政局房产税公告 ·
DHCS MC 216 白卡年度复审 · Kaiser 2026 ANOC。

**四封信第一次跑，两封挂。查下去一共五个 bug，没有一个是想出来的。**

| # | 危险 | 症状 | 根因 |
|---|---|---|---|
| 28 | 🔴 | 任何含 `medical` 的信都被说成白卡 Medi-Cal 寄来的 | `/medi-?cal\b/i` 连字符可选，而 `medical` 这个英文单词恰好就是 `medi`+`cal` |
| 29 | 🔴 | 锚点行自己带着日期，却去抓了下一行的日期 | `findValueNearAnchor` 里「正下方」85 分 > 「就在这一行」70 分 |
| 30 | 🔴 | 房产税单四个日期，取了**第二期**的 2025-02-01 | 只取分数最高的，同锚点同权重时排序纯看零头 |
| 31 | 🟠 | `is due in full on November 1` 一条锚点都不命中 | 分期账单的标准措辞不在锚点表里 |
| 32 | 🟠 | 官方 MC 216 复审表认不出子类型 | 词典只有旧版 `annual redetermination`，MC 216 写的是 `Your Medi-Cal is up for renewal` |

**28 号最值得说。** 加了 `i` 标志之后，`MediCal` 和 `medical` 在正则眼里是同一个
字符串，**没有任何办法区分**。所以只能要求分隔符 `/\bmedi[-\s]cal\b/i`。
代价是 OCR 吃掉连字符就漏认 —— 但漏认只是机构显示成「未知」，误认是自信地说错。
一封私立医院账单被告诉老人「这是白卡寄来的」，比认不出来危险得多。

**29 和 30 是同一件事的两半。** 29 让它看错了行，30 让它在多个日期里挑错了个。
两个都修完，房产税单才从「请在 2025年2月1日 之前处理」变成正确的 2024年11月1日。
新增校验 `due_date_single`：信上有多个到期日时记录下来，并取**今天之后最近的那个**
—— 老人要的永远是「下一个该做事的日子」，不是分数最高的那个日子。

**顺带修掉一个我自己刚引入的**：信头裸日期规则会抢走「Due Date | 12/10/2024」
里右边那个值框（它确实是一行光秃秃的日期，又在页面上方），于是同一个日期
既当截止日又当发信日期 —— 正是这轮要消灭的那种混淆。现在已被标签认领过的
值框不再参与信头判定。

**一个虚惊。** 调试时发现「标签和日期同一行」全部抽不出来，一度以为是大 bug。
实际是 `safeLines.length < 4` 的保护 —— 我的调试页只有 3 行。
**先怀疑自己的测试，再怀疑代码。**

---

## 2026-08-26 · 🐛 文档里的「已知机构 47」错了整整一轮

**症状** `docs` 和 `README` 都写着「47 known organizations」。真实数字是 **40**。

**根因** `update-docs.mjs` 是全文数 `/kind: '/`，而 `buildLayer0` 的 highlights 里
每个 `{ kind: 'amount' }`、`{ kind: 'dueDate' }`、以及 `kind: 'zero'`、`kind: 'autopay'`、
`kind: 'unknown'` 也长这样，全被数成了机构。

**发现于** 加发信日期高亮时，机构数从 47 跳到 48 —— 加一条高亮不可能多出一家机构。
**数字自己跳出来的，不是读代码看出来的。**

**修法**
- 计数限定在 `KNOWN_ORGS` 块里数，不再全文数
- 日期锚点改成 `DATE_ANCHORS + STATEMENT_DATE_ANCHORS` 两块相加
- 顺带修了分数行正则：原来的通配符写法在加了「发信日期」这一列之后
  先匹配上了**表头**，于是文档里的「当前分数」变成了一行表头
- **README 里手写的数字全部去掉**，分数改成 AUTO 块，和 `docs` 一样由脚本生成；
  `--check` 现在也检查 README

**教训（这条最要紧）**
这个脚本存在的全部理由就是「不让文档撒谎」，**结果它自己算错了一个数，还撒了一整轮**。

自动生成不等于正确 —— 它只保证「数字和某个算法一致」，不保证那个算法数的是对的东西。
所以计数这类代码，判据要**尽量窄**：数某个具体词典块里的条目，
而不是全文搜一个碰巧长得像的模式。

写这个脚本的时候我图省事全文数，因为「反正 kind: 只在机构表里出现」—— 当时是对的，
后来加了 highlights 就不对了，而它不会报错，只会安静地多数几个。
**「现在恰好成立」的假设，就是将来的 bug。**

---

## 2026-08-26 · ✅ 发信日期和截止日期分开抽、分开显示

**为什么** 这两个日期混在一起，是最容易害老人做错事的一类错。Hoag 那张医院账单上
只有 `STATEMENT DATE 10/19/18`、根本没有到期日 —— 混起来就等于凭空造出一个期限。

以前的做法是把 `statement date` / `bill date` 塞进 `DATE_ANCHOR_BLOCKERS` 整个挡掉。
挡住了误报，但也把一个真实存在、老人也想知道的信息一起扔了。

**改了什么**

- 新增 `STATEMENT_DATE_ANCHORS`（9 条）和 `STATEMENT_ANCHOR_BLOCKERS`
  （挡 date of birth / date of service —— 出生日期长得像发信日期，抽出来显示就是泄露）
- 收集循环里**先判发信日期再判截止日**，命中即 return，同一行不能兼任两职
- 新增 3 条交叉校验：置信度 · 发信日期不能在未来（留 2 天余量）· 发信日期不能晚于截止日
  （冲突时丢发信日期，因为截止日的锚点强得多、错了后果更重）
- 无标签的信头日期兜底（政府信、法院信常见）。卡死四条：整行只有日期 · 在页面上三分之一 ·
  不在未来 · 没有任何带标签的发信日期。**带标签的永远优先**
- 前端两个独立的框。截止日期红框、加「要在这天之前办」；发信日期灰底细边小字、
  加「不用在这天之前办什么」—— 光写「发信日期」四个字不够，那是行话
- `ground_truth.json` 加 `statement_date`（6 封全部重新看原图标注），
  `accuracy.test.mjs` 加第五个维度

**结果** 发信日期 5/6。前四维仍然 6/6，五个回归套件全绿。

**那一封漏掉的，是漏对了**
statefarm 保单页脚小字 `Prepared MAR 21 2023`，OCR 读成 `Ppeni MAR 21 2021` ——
锚点词烂了，年份还错了两年。抽不出来，所以不显示。
**显示一个差两年的日期，比什么都不显示糟糕得多。**

**顺带记两个已知缺口**
- `Bill Cycle Date: 01/25/19-01/27/19`（AT&T）是区间不是单个日期，不抽 —— 这是对的
- 一个 bug：`makeDateParser` 收的是整个 line 对象不是字符串，我第一版传了 `line.text`，
  静默失效、SCE 那封的信头日期一直抽不出来。**参数类型错了不会报错，只会安静地什么都不做**

---

## 2026-08-25 · 🐛 脱敏断言根本不在仓库里

**症状** `docs` 一直写着「脱敏断言 13 条」，实际 `contentRedactor.hoag.test.mjs`
从来没有落到仓库里 —— 它是在临时环境里跑的，机器一关就没了。
`phone_ocr.json` 同样缺失，`phone.test.mjs` 现在跑起来直接 `ENOENT`。

**发现于** 整理文档时逐个跑测试，不是靠读代码看出来的。
**这正是「文档会撒谎」的标准案例** —— 文档信誓旦旦地写着一层防线，那层防线并不存在。

**修法** 重写 `contentRedactor.hoag.test.mjs`，15 条断言，已入库并纳入
`update-docs.mjs` 的自动跑分。`phone.test.mjs` 的 fixture 仍然缺，
已在 `how-it-works.md` 里如实标注「要先自己生成」，没有假装它能跑。

**顺带发现的隐患（还没修）**
`findAddresseeBlock` 判断「这是机构地址、放行」用的是 `nearOrg(index, 3)` ——
机构名必须在 **3 行以内**。重写 fixture 时先按「医院地址和缴费信箱之间隔了 4 行」排版，
整个机构块（含缴费信箱、投递标签）**全被当成收件人地址挡掉了**。

后果是**挡过头**（🟠 翻译少一半），不是泄露，所以不紧急。
但真实账单版面千变万化，3 行这个窗口太脆。等接了本地 NER 之后一起重做这一段。

**教训** 「测试通过」和「测试存在」是两件事。
以后加一层防线，同一个 commit 里必须能看到那个文件。

---

## 2026-08 · ✅ v1 发布 —— 本地读信

第一个能用的版本。**整条链路零网络**：拍照 → 预处理 → OCR → 字段抽取 →
交叉校验 → 中文结论，全部在浏览器里完成。

**能做什么**

- 认出**信件类别**（电费/天然气/水费/车险/房屋险/白卡/红蓝卡/社安局/法院…）
  和**寄件机构**（有缩写用缩写）
- 抽取**金额**并区分三种状态：要交 / 自动扣款 / 不用交（含贷记余额）
- 抽取**截止日期**，并能正确地说「这封信没有截止日期」
- **红橙黄绿**四级紧急度，最紧急叠 `‼️`，提示语按真实原因生成
- 认出信件**子类型**（MSN / ANOC / 白卡年度复审 / HOA 留置权 / 陪审传票…）
- **诈骗特征**筛查
- **逐行脱敏判定**（六道关卡），算出哪些内容将来可以交给外部模型 —— 目前只计算不发送

（各词典的准确条目数见 `how-it-works.md` 的自动生成块，那里的数字才是当前值。）

**图像预处理** 纸张四角检测 → 透视矫正 → 去斜 → 去阴影 → 对比增强。
小图会放大（长边低于 1600，最多 3 倍）。不依赖 OpenCV，零新增下载体积。

**校验** 14 项交叉校验：分项求和、金额佐证、小数点合理性、日期陈旧判定、
类别内容分、锚点冲突、OCR 置信度等。**校验不过就说「看不准」，不硬猜。**

**测试** 真实账单 6 封 × 4 维度（人工标注真值）· 合成信件 17 · 官方 PDF 逐字原文 6 ·
类别拆分 9 · 脱敏断言 13 · 语句一致性自动断言 · 手机实拍路径 3 封 × 4 维度

**已知欠账** 见 [`../CLAUDE.md`](../CLAUDE.md) 的「下一步该做什么」。最要紧的三条：
PII 检测仍是正则 / 多页信件未处理 / 中文信件未测试。

---

## 2026-08 · 🐛 v1 期间修掉的 27 个 bug

**没有一个是想出来的，全部是被一张新样本炸出来的** —— 单是那封 AT&T 账单就贡献了 5 个。
所以这份台账不是历史档案，是**下一轮该测什么的线索**。

危险度：🔴 会让老人做错事 · 🟠 结果不可信 · 🟡 体验问题

### 抽取层（14 个）

错误的绝对主力。性质最危险 —— 产出的是**自信的错误答案**，不是「看不清」。

| # | 危险 | 症状 | 根因 | 发现于 |
|---|---|---|---|---|
| 1 | 🔴 | 真水费账单被告知「不用交钱」 | `DO NOT PAY` 命中「不是账单」规则，实际含义是「已自动扣款别重复付」 | water_bill |
| 2 | 🔴 | 保单写着 `AMOUNT DUE: None`，却说「要交 165 美元」 | `None` 不是数字，强锚点匹配失败，权重更低的 `Total Premium` 顶上 | statefarm |
| 3 | 🔴 | 水费单说「要交 71.20」，信上印着 `RECURRING PAYMENT - DO NOT PAY` | 金额只做了「要交/不用交」两态，缺「已自动扣款」第三态 | water_bill |
| 4 | 🔴 | 四张账单金额全部抓不到 | PP-OCR 输出**不带词间空格**，锚点正则写的是 `\s+` | 真实 PP-OCR |
| 5 | 🔴 | **任何四位数金额被截断**（`$1250`→125） | 小数部分可选，正则在 `\d{1,3}` 就能成功收工 | 小数点测试 |
| 6 | 🔴 | 从电话号码里凑出一个假截止日 | `Pay by Phone: 855-...` 命中日期锚点 | water_bill |
| 7 | 🟠 | 贷记余额 `$6.33CR` 被当成「没能确认金额」 | 负数不满足 `value > 0` | AT&T |
| 8 | 🟠 | `Payment is Not Required` 完全没匹配 | 只写了 `no payment is required`，语序反过来就废 | AT&T |
| 9 | 🟠 | 误报「已登记自动扣款」 | `$20 AutoPay discount`（营销）和 `CHECK FOR AUTO PAY`（空方框）都命中 | AT&T |
| 10 | 🟠 | 监管公函被判成「电费账单」 | 「机构是 SCE」单独给 8 分，刚好够 trusted 门槛 | SCE_Letter |
| 11 | 🟠 | 金额读对了却不敢用 | 佐证只认「分项求和/金额列/超强锚点」，OCR 把标签和金额并成一行时三条全落空 | Tesseract 路径 |
| 12 | 🟠 | `Sub-Total of ...` 只匹配到通用 `total`（权重 35） | 小计不在锚点表里 | 手机实拍 |
| 13 | 🟠 | 两年前的日期被当成待办 | 无陈旧判定 | statefarm |
| 14 | 🟠 | 分项求和不接受负数 | 写了 `sum > 0`，`-66.27 + 59.94 = -6.33` 对得上却被跳过 | AT&T |

### 识别层（5 个）

多数无害 —— 字符错误集中在长句子，而长句子不参与抽取。

| # | 危险 | 症状 | 根因 | 发现于 |
|---|---|---|---|---|
| 15 | 🔴 | `$59.94` → `$5994`，**置信度 100%** | 小数点丢失，放大 100 倍，靠置信度筛不掉 | AT&T |
| 16 | 🟠 | `$165.00` → `$165,00` | 小数点认成千分位逗号 | statefarm |
| 17 | 🟠 | 小图完全读不出金额 | 预处理只降不升，727×294 原样送 OCR | hoag |
| 18 | 🟠 | 细笔画被打伤（命中 3/4→2/4） | OCR 用图用 JPEG 0.95 有损压缩 | hoag |
| 19 | 🟡 | `Clean Power` → `Lean Power` | 普通字符错误，不影响抽取 | SCE |

### 表达层（4 个）

| # | 危险 | 症状 | 根因 | 发现于 |
|---|---|---|---|---|
| 20 | 🔴 | 法院信被说成「没有发现特别紧急的措辞」 | 词典未命中时的兜底文案把「没读懂」说成「不紧急」 | 法院传票用例 |
| 21 | 🟠 | 「确认了金额和日期」，下一行「没有找到截止日期」 | 模板句写死，陈述未验证的事实 | Hoag（用户发现） |
| 22 | 🟠 | 提示语「有明确期限」但没找到期限 | 提示按等级套现成的话，不按真实原因 | 用户发现 |
| 23 | 🟡 | 「有几项没看准」和上方小标题重复 | — | 用户发现 |

### 脱敏层（4 个）

| # | 危险 | 症状 | 根因 | 发现于 |
|---|---|---|---|---|
| 24 | 🔴 | **账号和服务地址被判为机构信息放行** | 拿宽词表 `ORG_HINTS`（含 `service`）当机构锚点，「Service account」被认成机构名 | SCE |
| 25 | 🔴 | 真名 `BAKER, NATE` 漏了，`Generation Charges` 反被当人名拦 | 纯形状判断，未配文档常用词表 | SCE |
| 26 | 🟠 | 缴费信箱、医院电话全被挡 | `ORG_HINTS` 漏 `institute`，整个机构块被当收件人地址 | Hoag |
| 27 | 🟠 | 金额行被当成收件人区块的一部分挡掉 | 地址块盲目扩 3 行 | Hoag |

### 从台账里读出来的四条规律

**1 · 同一个意思，英文有无数种写法。**
`no payment required` / `Payment is Not Required` / `AMOUNT DUE: None` / `$0.00` /
`DO NOT PAY` / `Nothing is due` —— 每次只覆盖了当时想到的那几种。**这是无底洞。**

**2 · 同一个词组，在不同上下文意思相反。**

| 原文 | 真实含义 | 曾经的误解 |
|---|---|---|
| `DO NOT PAY` | 已自动扣款 | 不是账单 ❌ |
| `Pay by Phone` | 缴费方式 | 缴费期限 ❌ |
| `AutoPay discount` | 营销话术 | 已登记自动扣款 ❌ |

**这类最难防，字面完全正确，只有上下文能区分 —— 也最适合交给 LLM。**

**3 · 自己写的正则 bug 只能靠样本暴露。**
四位数金额截断（#5）存在了整轮，六张测试图恰好都是三位数金额，运气好没撞上。

**4 · 用户发现的 bug 全在表达层。**
#21 #22 #23 都是用户读输出时发现的自相矛盾。
**这说明表达层需要的是「读一遍」，而不是更多测试** —— 现在有一致性断言在防。

---

## 2026-08 · ⚖️ 决定 04 · 默认零网络，联网是用户主动开启的增强

**背景** 同类产品（如 App Store 上的 AdminGuide）的架构是：默认把照片传到服务器分析，
本地处理是较新 iPhone 上的加分项。它的隐私标签申报收集 `User Content（照片）`、
`Financial Info` 并**关联到身份**。

**决定** 方向反过来：默认全部在设备上完成，联网是用户主动打开的增强功能。配套三条：

1. **不做账号** —— 没有 user ID，结构上就没有身份可以关联
2. **飞行模式可用** —— 当成功能来测试和宣传
3. **每条回答标出来源** —— 本机生成 / 需要联网，逐条可见

**理由**

1. **老人用的是旧手机。**「较新的 iPhone 可以完全本地处理」反过来读就是：旧手机照样上传。
   而这个产品的用户，恰恰是拿着儿女淘汰下来的旧手机的老人 ——
   **最需要保护的那群人走的是上传那条路。**
2. **默认设置基本没人改。** 老人不会去翻设置。默认是什么，实际就是什么。
   所以「有没有本地模式」不重要，**「默认走哪条」才重要**。
3. **可验证 > 可信任。**「我们用加密连接传输」没法验证，只能选择相信；
   「飞行模式下功能照常」五秒钟就能自己确认。对老人本人可能无所谓，
   **但决定装不装的是子女**。
4. **无账号是结构性保证。**「我们承诺不关联身份」和「我们没有身份可以关联」，
   差的不是程度，是性质。

**代价** 首次加载要下模型，比服务端方案慢；老手机性能吃紧；
没有账号意味着没有跨设备同步、没有用户行为数据。

**什么情况下该推翻** **这一条不该被推翻** —— 它是产品定位本身。
如果哪天必须默认上传，那就不再是这个产品了，应该另起一个名字。

---

## 2026-08 · ⚖️ 决定 03 · 知识在构建期生成，不在运行期调用

**背景** 老人需要知道「白卡年度复审要做什么」「HOA 留置权通知有多严重」。
本地模板词典覆盖不了长尾。自然的想法是运行时调 LLM。

**决定** 把 LLM 用在构建期，不用在运行期。
离线跑一次模型，把常见信件类型（估计 200–300 种）的解释全部生成出来，
人工审核后打包成 `knowledge.json`（约 1–3 MB）随 app 发布。
运行时本地分类 → 查表 → 注入本地验证过的金额日期 → 渲染。

**理由**

1. **这些知识是有限、静态、与用户无关的。** 白卡复审的规则今年和明年一样、
   张三和李四一样 —— 模型根本不需要看这封信就知道。
2. **运行期零网络。** 内容质量是 LLM 级的，但一个字节都不出设备。
   这直接支撑了决定 04 的可验证性。
3. **可审核。** 法院传票、移民补件、医保终止 —— 这类内容的建议**应该在发布前被人看过一遍**，
   而不是每次现场由模型即兴生成。对老人这个用户群体，可审核比实时更负责任。
4. **不会幻觉，可回归测试。** 同一类信永远同一个答案。这一点反复吃过亏（见表达层 bug）。
5. **零边际成本。** 没有 API 账单，所以能做真正慷慨的免费版 ——
   这是走非营利和老人中心渠道的前提。同类产品收订阅费，是因为它每次分析都有推理成本，
   **它想免费也免费不起**。

**代价** 打不中的类型只能说「这封信我不认识」；回答不了实例级问题
（「为什么这个月比上月贵 40 块」）；知识库需要随法规变化维护。

**什么情况下该推翻** 实例级问题的真实需求比预期高得多时。
但即使那时，也应该是**在预生成之外增加一条路径**，而不是替换掉它 ——
预生成那条路的离线和可审核性质是独立价值。

---

## 2026-08 · ⚖️ 决定 02 · 脱敏用白名单方向，不用黑名单

**背景** 要把信件内容交给外部模型，就得先脱敏。两种方向：

- **黑名单**：默认全发，检测到 PII 才挡。漏检一次 = 泄露一次。
- **白名单**：默认不发，只发明确认定安全的。漏检一次 = 少发一点内容。

**决定** 白名单方向。逐行设六道关卡，全过才允许外发。
同时**不把「发送前请用户确认」当作安全机制**。

**理由**

1. **用户看不懂英文，没人能兜底。** 黑名单的安全性押在「漏检会被人发现」上。
   而这个产品的用户**看不懂英文**，所以没人会发现。
   **兜底的人不存在，那就不能有需要兜底的设计。**
   （发送前可见的面板仍然保留，但它的定位是**知情**，不是**安全**。）
2. **把风险从「隐私事故」降级成「体验瑕疵」。**
   白名单下漏检的代价是「翻译不完整」，不是「账号被发出去」。
3. **PII 检测没有验算办法。** 金额错了有分项求和能拦。
   **人名漏了，没有任何校验和能验证它。** 这是整个系统里唯一一个
   「漏了没有任何人会发现」的模块，所以必须在方向上倒向保守。

**代价** 外发内容覆盖率 58%–91%（视信件而定），保险 EOB 只有 50%。
覆盖率太低时，模型可能拼不出完整意思。

**什么情况下该推翻** 不该推翻方向，但**闸门可以放松**，前提是先接入本地 NER 模型
把人名地址的召回率提上来。规则和模型并联、任一命中即挡之后，
才有资格讨论放宽哪几条关卡。

---

## 2026-08 · ⚖️ 决定 01 · 金额和日期永远由本地抽取，不交给 LLM

**背景** 接入 LLM 之后，一个自然的想法是「本地没看清的字段让模型补」。
模型确实能从图或文字里读出金额。

**决定** **LLM 永远不产出金额和日期。** 这两个字段只能来自本地抽取 + 本地校验。
即使本地失败，也宁可显示「没看准」，不让模型填。
前端渲染时，数字从 `letterFields` 取，**不从模型响应里取**。

**理由**

1. **本地有验算，模型没有。** `33.94 + 35.74 + 25.22 + 4.27 + 0.19 = 99.36`
   —— 这是算术证明。模型给一个数字，没有第二条路径能核对它。
2. **读数字是 LLM 最弱的能力，却是最高风险的位置。**
   转录数字这件事 OCR + 正则又快又可验证；模型的优势是理解，不是转录。
3. **结构上杜绝幻觉。** 数字压根不经过模型，即使模型幻觉了也不可能编错金额。
   这比在 prompt 里求它「不要编造数字」可靠得多。
4. **相关性陷阱。** 本地抽取和本地脱敏跑在同一份 OCR 上。OCR 烂到抽不出金额时，
   它同样烂到 PII 检测认不准 ——
   **「本地失败时才调 LLM」等于「恰好在脱敏最不可靠时把数据发出去」**。

**代价** 本地读不出金额时，老人得到的是「没看准」而不是一个数字。
体验上是退步，但这是有意的：**说不知道，比自信地说错好。**

**什么情况下该推翻**

- 出现了能让模型输出**可验证**的机制（比如模型同时给出它读到的分项，本地能验算）
- 或者积累了足够数据证明模型在这个任务上的错误率低于本地抽取**一个数量级以上**
